/** It's a program where we print
 * the phrase: with proper grammar, but the outcome is a piece of art,
 */
#include<stdio.h>
int main()
{
/* A function that prints this phrase */
        printf("with proper grammar, but the outcome is a piece of art,");
        return (0);
}
